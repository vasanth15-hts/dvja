version: 0.2

phases:
  install:
    commands:
      - echo "Updating APT packages and installing dependencies"
      - apt-get update -y && apt-get install -y curl unzip
      
      - echo "Installing Node.js (with npm)"
      - curl -sL https://deb.nodesource.com/setup_20.x | bash - && apt-get install -y nodejs
      
      - node -v
      - npm -v

      - echo "Installing OWASP Dependency Check CLI"
      - curl -L https://github.com/jeremylong/DependencyCheck/releases/download/v8.3.1/dependency-check-8.3.1-release.zip -o dependency-check.zip
      - unzip dependency-check.zip -d dependency-check
      - export PATH=$PATH:$(pwd)/dependency-check/bin

      - echo "Installing Snyk CLI and Snyk to HTML converter"
      - npm install -g snyk snyk-to-html --no-color
      - snyk --version

  pre_build:
    commands:
      - echo "Authenticating with Snyk"
      - snyk auth $SNYK_TOKEN

  build:
    commands:
      - echo "Running Maven Clean and Package"
      - mvn clean package

      - echo "Running OWASP Dependency Check with NVD API Key"
      - mvn org.owasp:dependency-check-maven:check -Dnvd.api.key=$NVD_API_KEY

      - echo "Running Snyk for SAST and saving report"
      - snyk test --all-projects --json > snyk-report.json || true

      - echo "Converting Snyk JSON report to HTML"
      - snyk-to-html -i snyk-report.json -o snyk-report.html

      - echo "Moving Snyk report to target directory"
      - mv snyk-report.html target/

      - echo "Generating image definition file"
      - echo '[{"name":"cicd/test","imageUri":"890742607967.dkr.ecr.ap-south-1.amazonaws.com/cicd/test:latest"}]' > target/imagedefinitions.json

      - echo "Running SonarCloud Analysis"
      - mvn verify sonar:sonar -Dsonar.projectKey=monster -Dsonar.organization=monster -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=9694b8a21216ed240b16f68817210bf76422579f

  post_build:
    commands:
      - echo "Zipping all reports in target folder..."
      - zip -r reports.zip target/*

      - echo "Uploading reports.zip to S3..."
      - aws s3 cp reports.zip s3://cicddevsecops/reports.zip
      
      - echo "Generating pre-signed URL for reports.zip..."
      - export PRESIGNED_URL=$(aws s3 presign s3://cicddevsecops/reports.zip --expires-in 3600)

      - echo "Sending email with pre-signed URL..."
      - |
        aws ses send-email \
          --from "vasanth@hts.consulting" \
          --destination "ToAddresses=antony@hts.consulting,vasanth@hts.consulting" \
          --message "Subject=DevSecOps Reports, Body=Attached are the reports generated from the DevSecOps pipeline. You can download them here: $PRESIGNED_URL" \
          --region "ap-south-1"

artifacts:
  files:
    - target/*
    - target/imagedefinitions.json  # Include image definition file

reports:
  dependency-check:
    files:
      - target/dependency-check-report.html
  snyk:
    files:
      - target/snyk-report.html

cache:
  paths:
    - /root/.m2/**/*  # Cache Maven dependencies
